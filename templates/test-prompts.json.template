{
  "skill": "{{skill-slug}}",
  "version": "0.1.0",
  "source_book": "{{BOOK_TITLE}} — {{AUTHOR}}",
  "darwin_compatible": true,
  "test_cases": [
    {
      "id": "should-trigger-01",
      "type": "should_trigger",
      "prompt": "{{Typical User Phrases 1 — Positive Scenario}}",
      "expected_behavior": "Skill slug should be activated, and the specific action expected should be performed."
      "notes": "{{Why is this a positive scene}}"
    },
    {
      "id": "should-trigger-02",
      "type": "should_trigger",
      "prompt": "{{Typical User Phrases 2}}",
      "expected_behavior": "...",
      "notes": "..."
    },
    {
      "id": "should-trigger-03",
      "type": "should_trigger",
      "prompt": "{{Typical User Phrases 3}}",
      "expected_behavior": "...",
      "notes": "..."
    },
    {
      "id": "should-not-trigger-01",
      "type": "should_not_trigger",
      "prompt": "{{Decoy 1 — Seemingly relevant but should not actually be called}}",
      "expected_behavior": "This skill should not be activated because {{reason}}",
      Notes: "Lure: {{What type of lure is this?}}"
    },
    {
      "id": "should-not-trigger-02",
      "type": "should_not_trigger",
      "prompt": "{{Decoy 2 — Another situation where it shouldn't be called}}",
      "expected_behavior": "...",
      "notes": "..."
    },
    {
      "id": "edge-01",
      "type": "edge_case",
      "prompt": "{{Blurred boundary scene}}",
      "expected_behavior": "{{A reasonable judgment of expectation—it might be that the function is called, or it might not be called, but the reason must be clearly explained}}",
      Notes: Boundary: {{Why is this a boundary?}}
    }
  ],
  "minimum_pass_rate": 0.8,
  Notes: "At least 3 should_triggers + 2 should_not_triggers + 1 edge_case. All should_not_triggers must pass (decoy tolerance is 0)."
}
